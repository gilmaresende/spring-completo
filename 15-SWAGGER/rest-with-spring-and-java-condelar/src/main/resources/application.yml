spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/full-java?useTimezone=true&serverTimezone=UTC
    username: root
    password: Idev
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      show-sql: false
spring-doc:
  pathToMatch: /api/**/v1/**
  swagger-ui: 
    use-root-path: true